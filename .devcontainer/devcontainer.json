{
  "name": "Laravel Dev Environment",
  
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/site-laravel",

  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "mikestead.dotenv"
      ]
    }
  },


  "postCreateCommand": "composer update && composer install && npm install && php artisan key:generate",
}