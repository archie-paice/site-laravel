; Only start a debug session when explicitly triggered (IDE request / XDEBUG_TRIGGER),
; so plain CLI and web requests don't spam "Could not connect to debugging client".
xdebug.start_with_request = trigger
; Xdebug 3 default port, matching VS Code php-debug and PhpStorm defaults.
xdebug.client_port = 9003
